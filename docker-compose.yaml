version: '3.7' # TODO: upgrade to 3.8

services:
  # vault:
  #   container_name: vault
  #   build:
  #     context: ./vault
  #     dockerfile: Dockerfile
  #   ports:
  #     - 8200:8200
  #   volumes:
  #     - ./vault/config:/vault/config
  #     - ./vault/policies:/vault/policies
  #     # - ./vault/data:/vault/data
  #     # - ./vault/logs:/vault/logs
  #   environment:
  #     - VAULT_ADDR=http://127.0.0.1:8200
  #     - VAULT_API_ADDR=http://127.0.0.1:8200
  #   command: server -config=/vault/config/vault-config.json
  #   cap_add:
  #     - IPC_LOCK
  #   depends_on:
  #     - consul

  echo-server:
    image: sbrakl/flask:latest
    # environment:
    #   - ENABLE__ENVIRONMENT=false
    # ports:
    #   - 5000:5000
    expose:
      - "5000"

  # consul:
  #   container_name: consul
  #   build:
  #     context: ./consul
  #     dockerfile: Dockerfile
  #   # ports:
  #   #   - 8500:8500
  #   expose:
  #     - "8500"
  #   command: agent -server -bind 0.0.0.0 -client 0.0.0.0 -bootstrap-expect 1 -config-file=/consul/config/config.json
  #   volumes:
  #     - ./consul/config/consul-config.json:/consul/config/config.json
  #     # - ./consul/data:/consul/data

  # consul-worker:
  #   build:
  #     # contailer_name: Can't use a container_name when scaling
  #     context: ./consul
  #     dockerfile: Dockerfile
  #   expose:
  #   - "8500"
  #   command: agent -server -join consul -config-file=/consul/config/config.json
  #   volumes:
  #     - ./consul/config/consul-config.json:/consul/config/config.json
  #   depends_on:
  #     - consul

  nginx:
    container_name: consul_lb
    image: nginx:alpine
    ports:
      - 8500:8500
    volumes:
      - ./nginx-proxy/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      #- consul
      - echo-server
